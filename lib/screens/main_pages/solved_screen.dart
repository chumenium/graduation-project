import 'package:flutter/material.dart';

class SolvedScreen extends StatelessWidget {
  const SolvedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('解決したい画面')),
    );
  }
}